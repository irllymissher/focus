<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos y Hardware para la Cámara -->
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="true" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".FocusFlowApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FocusFlowSinBackend"
        tools:replace="android:allowBackup">

        <!-- 1. Pantalla de Inicio: EL LANZADOR -->
        <activity
            android:name=".InicioActivity"
            android:theme="@style/Theme.FocusFlowSinBackend"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 2. Flujo de Sesión y Evaluación -->
        <activity android:name=".EscaneoActivity" />
        <activity android:name=".SesionActivity" />
        <activity android:name=".EvaluacionActivity" />

        <!-- 3. Pantalla de Selección de Descanso-->
        <activity android:name=".DescansoActivity" />

        <!-- 4. Actividades de Descanso Específicas-->
        <activity android:name=".GpsActivity" />
        <activity android:name=".MentalActivity" />
        <activity android:name=".EstiramientosActivity" />
        <activity android:name=".AguaActivity" />
        <activity android:name=".SnacksActivity" />
        <activity android:name=".RespiracionActivity" />

        <!-- 5. Pantalla de Resumen Final -->
        <activity android:name=".ResumenActivity" />

        <!-- PROVEEDOR DE ARCHIVOS: Vital para que GestorFotosAndroidImpl funcione -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>