<resources>
    <!-- Si quieres mantener el Splash, asegúrate de que la librería del paso 2 esté instalada -->
    <style name="Theme.SplashScreen" parent="Theme.SplashScreen.IconBackground">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_round</item>
        <item name="postSplashScreenTheme">@style/Theme.FocusFlowSinBackend</item>
    </style>

    <style name="Theme.FocusFlowSinBackend" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Usa los colores que acabamos de restaurar -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
    </style>
</resources>