<?xml version="1.0" encoding="utf-8"?>
<!-- Pantalla 4.3: Estiramientos — Nivel 3 -->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_dark"
    android:paddingHorizontal="22dp">

    <!-- Toolbar -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        app:navigationIcon="@drawable/ic_arrow_back"
        app:navigationIconTint="@color/text_muted"
        app:title="@string/screen_estiramientos"
        app:titleTextColor="@color/text_muted"
        app:titleTextAppearance="@style/TextAppearance.TopBar"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- Barra de progreso por pasos (4 pasos) -->
    <LinearLayout
        android:id="@+id/ll_steps"
        android:layout_width="match_parent"
        android:layout_height="4dp"
        android:orientation="horizontal"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/toolbar"
        app:layout_constraintStart_toStartOf="parent">

        <View android:id="@+id/step1" android:layout_width="0dp" android:layout_height="4dp"
            android:layout_weight="1" android:layout_marginEnd="6dp"
            android:background="@drawable/bg_step_active" />
        <View android:id="@+id/step2" android:layout_width="0dp" android:layout_height="4dp"
            android:layout_weight="1" android:layout_marginEnd="6dp"
            android:background="@drawable/bg_step_inactive" />
        <View android:id="@+id/step3" android:layout_width="0dp" android:layout_height="4dp"
            android:layout_weight="1" android:layout_marginEnd="6dp"
            android:background="@drawable/bg_step_inactive" />
        <View android:id="@+id/step4" android:layout_width="0dp" android:layout_height="4dp"
            android:layout_weight="1"
            android:background="@drawable/bg_step_inactive" />
    </LinearLayout>

    <!-- Emoji del ejercicio (animado con ScaleAnimation) -->
    <TextView
        android:id="@+id/tv_stretch_emoji"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🧘"
        android:textSize="72sp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintVertical_bias="0.35" />

    <!-- Nombre del estiramiento -->
    <TextView
        android:id="@+id/tv_stretch_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Cuello"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="@color/text_primary"
        android:gravity="center"
        android:layout_marginTop="20dp"
        app:layout_constraintTop_toBottomOf="@id/tv_stretch_emoji"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- Descripción -->
    <TextView
        android:id="@+id/tv_stretch_desc"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Inclina la cabeza al lado derecho · 30s"
        android:textSize="15sp"
        android:textColor="@color/text_muted"
        android:gravity="center"
        android:lineSpacingMultiplier="1.5"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/tv_stretch_name"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- Tu turno -->
    <TextView
        android:id="@+id/tv_your_turn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/your_turn"
        android:textSize="12sp"
        android:textColor="@color/text_dim"
        android:gravity="center"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/tv_stretch_desc"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- Botón siguiente / completado -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_next_stretch"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:text="@string/btn_next"
        android:textSize="15sp"
        android:textStyle="bold"
        android:backgroundTint="@color/orange_primary"
        app:cornerRadius="16dp"
        android:layout_marginBottom="10dp"
        app:layout_constraintBottom_toTopOf="@id/btn_prev_stretch"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- Botón anterior (GONE en el paso 1) -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_prev_stretch"
        style="@style/Widget.MaterialComponents.Button.OutlinedButton"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:text="@string/btn_prev"
        android:textSize="15sp"
        android:textColor="@color/text_muted"
        app:strokeColor="@color/border"
        app:cornerRadius="16dp"
        android:layout_marginBottom="32dp"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
